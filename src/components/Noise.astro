<svg class='mix-blend-multiply' viewBox='0 0 600 600'>
  <filter id='noise-filter'>
    <feTurbulence baseFrequency='2'></feTurbulence>
  </filter>
  <rect width='100%' height='100%' filter='url(#noise-filter)'></rect>
</svg>
