---
import Badge from '@/components/ui/Badge.astro'

interface Props {
  stack: Array<string>
}

const { stack } = Astro.props
---

<ui class='flex flex-wrap gap-4'>
  {stack.map((tech) => <Badge label={tech} />)}
</ui>
